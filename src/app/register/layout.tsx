export const metadata = {
	title: 'CookMate | Register',
	description:
		'Register for CookMate to start discovering personalized meal recommendations, manage your favorite recipes, and enjoy a tailored culinary experience. Sign up now to join our community and explore new recipes.',
};

export default function Layout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return <>{children}</>;
}
